[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mari-substrates"
version = "0.1.0.dev0"
description = "Provider-neutral ports for external knowledge and connector substrates"
requires-python = ">=3.11"
dependencies = ["mari-core==0.1.0.dev0"]

[tool.setuptools.packages.find]
where = ["src"]
